//wap to check if a number is divisibe by 2 or not

#include<stdio.h>

int main(){
    int num=4;
    if(num%2==0){
        printf("%d is divisible by 2",num);
        }
        else{
            printf("%d is not divisible by 2",num);
            }
    return 0;
}